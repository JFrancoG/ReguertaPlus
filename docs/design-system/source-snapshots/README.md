# Source Snapshots

This folder stores imported reference material from the current Android and iOS design systems.

Purpose:

- Preserve traceability to the original state.
- Support migration and naming cleanup decisions.
- Avoid re-discovery cost during implementation.

Files:

- `current-android-design-system.md`
- `current-ios-design-system.md`
- `current-design-system-files-inventory.md`

Notes:

- These snapshots are reference input, not immutable rules.
- Canonical rules live in parent documents (`foundations`, `components`, `governance`).
